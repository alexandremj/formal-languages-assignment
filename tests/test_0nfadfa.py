from application.dfa import DFA
from application.nfa import NFA

""" Test if, given a DFA, after executing the algorithm the result will be the
    same DFA"""
def test_nfa_dfa_conversion_input_dfa(dfa_creation):
    pass
    